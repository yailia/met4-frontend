---
const base = import.meta.env.BASE_URL;
const year = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-section">
        <h3>О компании МЭТЧ</h3>
        <p>Повышаем эффективность компании через устойчивость команд</p>
      </div>
      <div class="footer-section">
        <h3>Контакты</h3>
        <p><a href="mailto:hello@met4.ru" class="footer-contact-link">hello@met4.ru</a></p>
        <p><a href="tel:+79001234567" class="footer-contact-link">+7 (900) 123-45-67</a></p>
        <p><a href={`${base}contact/`} class="footer-contact-link">Форма связи</a></p>
      </div>
      <div class="footer-section">
        <h3>Следите за нами</h3>
        <div class="footer-social">
          <a href="https://t.me/met4_ru" class="social-link" target="_blank" rel="noopener noreferrer">Telegram</a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; {year} МЭТЧ. Все права защищены.</p>
      <a href={`${base}privacy/`} class="footer-link">Политика конфиденциальности</a>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: rgba(31, 42, 36, 0.95);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    color: rgba(255, 255, 255, 0.9);
    padding: var(--spacing-3xl) 0 var(--spacing-xl);
    margin-top: var(--spacing-4xl);
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-2xl);
    margin-bottom: var(--spacing-2xl);
  }

  @media (min-width: 768px) {
    .footer-content {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-xl);
    }
  }

  .footer-section h3 {
    color: #fff;
    margin-bottom: var(--spacing-md);
    font-size: var(--text-lg);
  }

  .footer-section p {
    color: rgba(255, 255, 255, 0.7);
    font-size: var(--text-sm);
    line-height: var(--lh-loose);
  }

  .footer-contact-link {
    color: #d4b8ff;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .footer-contact-link:hover {
    color: #fff;
  }

  .footer-social {
    display: flex;
    gap: var(--spacing-md);
  }

  .social-link {
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color 0.2s ease;
  }

  .social-link:hover {
    color: #fff;
  }

  .footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: var(--spacing-xl);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
    align-items: center;
    text-align: center;
  }

  @media (min-width: 768px) {
    .footer-bottom {
      flex-direction: row;
      justify-content: space-between;
    }
  }

  .footer-bottom p {
    color: rgba(255, 255, 255, 0.6);
    font-size: var(--text-xs);
    margin: 0;
  }

  .footer-link {
    color: rgba(255, 255, 255, 0.7);
    text-decoration: none;
    font-size: var(--text-xs);
    transition: color 0.2s ease;
  }

  .footer-link:hover {
    color: #fff;
  }
</style>
